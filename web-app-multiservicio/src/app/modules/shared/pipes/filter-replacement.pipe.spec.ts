import { FilterReplacementPipe } from './filter-replacement.pipe';

describe('FilterReplacementPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReplacementPipe();
    expect(pipe).toBeTruthy();
  });
});
