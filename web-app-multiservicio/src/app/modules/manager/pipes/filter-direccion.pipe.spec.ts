import { FilterDireccionPipe } from './filter-direccion.pipe';

describe('FilterDireccionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDireccionPipe();
    expect(pipe).toBeTruthy();
  });
});
