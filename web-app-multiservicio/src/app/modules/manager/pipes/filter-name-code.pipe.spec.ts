import { FilterNameCodePipe } from './filter-name-code.pipe';

describe('FilterNameCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameCodePipe();
    expect(pipe).toBeTruthy();
  });
});
