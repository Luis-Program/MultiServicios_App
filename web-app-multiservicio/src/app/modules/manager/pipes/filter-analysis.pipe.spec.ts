import { FilterAnalysisPipe } from './filter-analysis.pipe';

describe('FilterAnalysisPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnalysisPipe();
    expect(pipe).toBeTruthy();
  });
});
