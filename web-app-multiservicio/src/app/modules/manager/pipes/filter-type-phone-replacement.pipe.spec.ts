import { FilterTypePhoneReplacementPipe } from './filter-type-phone-replacement.pipe';

describe('FilterTypePhoneReplacementPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTypePhoneReplacementPipe();
    expect(pipe).toBeTruthy();
  });
});
