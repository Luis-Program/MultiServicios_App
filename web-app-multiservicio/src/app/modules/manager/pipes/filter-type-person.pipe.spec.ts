import { FilterTypePersonPipe } from './filter-type-person.pipe';

describe('FilterTypePersonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTypePersonPipe();
    expect(pipe).toBeTruthy();
  });
});
