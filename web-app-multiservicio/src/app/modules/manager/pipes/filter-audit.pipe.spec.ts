import { FilterAuditPipe } from './filter-audit.pipe';

describe('FilterAuditPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAuditPipe();
    expect(pipe).toBeTruthy();
  });
});
