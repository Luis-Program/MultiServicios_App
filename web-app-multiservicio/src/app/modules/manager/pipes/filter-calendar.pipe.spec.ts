import { FilterCalendarPipe } from './filter-calendar.pipe';

describe('FilterCalendarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCalendarPipe();
    expect(pipe).toBeTruthy();
  });
});
