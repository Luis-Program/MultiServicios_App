import { FilterBusinessPipe } from './filter-business.pipe';

describe('FilterBusinessPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBusinessPipe();
    expect(pipe).toBeTruthy();
  });
});
